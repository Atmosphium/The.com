/*const toggleSwitch = document.querySelector(
  '.theme-switch input[type="checkbox"]'
);
const currentTheme = localStorage.getItem("temaa");

if (currentTheme) {
  document.documentElement.setAttribute("data-temaa", currentTheme);

  if (currentTheme === "dark") {
    toggleSwitch.checked = true;
  }
}

function switchTheme(e) {
  if (e.target.checked) {
    document.documentElement.setAttribute("data-temaa", "dark");
    localStorage.setItem("temaa", "dark");
  } else {
    document.documentElement.setAttribute("data-temaa", "light");
    localStorage.setItem("temaa", "light");
  }
}

toggleSwitch.addEventListener("change", switchTheme, false);
*/